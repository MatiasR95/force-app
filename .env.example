# Apps Script Web App URL (gym account). Leave empty to run in demo mode.
# The real URL lives ONLY in .env.local (untracked) and in the GitHub Actions
# secret VITE_FORCE_API — never commit it here (public repo).
# VITE_FORCE_API=https://script.google.com/macros/s/XXXXXXXX/exec
VITE_FORCE_API=
